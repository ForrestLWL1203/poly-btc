# credentials / secrets — NEVER commit (public repo)
secret/
*.pem
*.key
.env
.env.*

# local collection data
data/
*.db
*.db-wal
*.db-shm

# preview-only vendored libs (build-free React/Babel; re-copyable, will go when we move to Vite)
web/vendor/

# python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.mypy_cache/

# private strategy notes — remove this line if you want it public
doc/
btc5min-handoff.md
monitor-plan.md
findings.md

# private operational runbook (contains VPS host / ops) — never public
CLAUDE.md

# macOS
.DS_Store
